// ES6 버전에서 배열 관련 함수가 추가되었음

// forEach() : 지정한 배열의 요소에 callback을 지정한 함수의 내용을 실행하는 함수, 반환값이 없음
// 사용법 :
// 배열.forEach(콜백함수(현재값이 저장될 변수, 현재 index, 사용한 배열명)) {
//  실행할 소스코드
// }

// map() : forEach()와 같이 지정한 배열의 요소에 callback으로 지정한 함수의 내용을 실행하고 결과값을 배열로 반환하는 함수
// 배열.map(콜백함수(현재값이 저장될 변수, 현재 index, 사용한 배열명)) {
//  실행할 소스코드
//  return 반환값
// }
